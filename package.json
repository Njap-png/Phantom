{
  "name": "phantom",
  "version": "0.2.0",
  "description": "Space-evolving multi-agent terminal UI — works on desktop & Termux",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "phantom": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "clean": "rm -rf dist"
  },
  "keywords": ["agent", "terminal", "ai", "multi-agent", "termux"],
  "license": "MIT",
  "dependencies": {
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
